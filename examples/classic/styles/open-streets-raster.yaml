scene:
  background:
    color: '#e8e5df'

sources:
  basemap:
    type: Raster
    url: https://tile.openstreetmap.org/{z}/{x}/{y}.png
    max_zoom: 19

layers:
  basemap:
    data: {source: basemap}
    draw:
      raster:
        order: 0
